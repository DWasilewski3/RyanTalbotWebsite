version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo 'Pre-build phase'
    build:
      commands:
        - echo 'Build complete'
  artifacts:
    baseDirectory: '.'
    files:
      - '**/*'
  cache:
    paths: []
